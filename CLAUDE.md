# 프로젝트 공통 규칙

## 역할 및 담당 폴더
- backend/, api/  → 백엔드(기능구현) 세션 담당
- src/components/, src/styles/  → 프론트엔드(디자인) 세션 담당
- 자신의 담당 폴더 외에는 수정하지 않는다
- (실제 폴더명이 다르면 이 목록을 프로젝트 구조에 맞게 수정할 것)

## 파일 구조 및 코드 관리 원칙
- 코드 작성 전 폴더/파일 구조부터 설계하고 시작한다. 파일을 무작정 늘어놓지 않고 처음부터 논리적인 디렉토리 구조를 잡은 뒤 코드를 작성한다.
- 불필요한 코드(중복 로직, 사용하지 않는 함수/변수/주석 처리된 코드 등)는 최소화한다.
- 기존 코드를 수정하면서 더 이상 필요 없어진 코드(구버전 함수, 대체된 로직, 남은 import 등)는 남겨두지 말고 함께 삭제한다.

## 코딩 컨벤션
- (사용 언어, 프레임워크, 네이밍 규칙 등 프로젝트에 맞게 기입)

## 커밋 규칙
- 작업 단위로 커밋한다
- 커밋 메시지 접두사: 백엔드는 [BE], 프론트엔드는 [FE]

## API 변경 시
- API_CONTRACT.md를 반드시 함께 업데이트한다
- 변경 내용은 상대 담당 세션에 전달되어야 하므로, 변경 사항을 커밋 메시지에도 명확히 기록한다

## 패키지 설치
- npm, pip 등 새 패키지 설치가 필요한 경우, 임의로 설치하지 말고 먼저 사용자에게 설치 여부를 확인한다
